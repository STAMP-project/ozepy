buildchains:
- chain:
  - {rule: Apache}
  - {name: ubuntu, tag: latest}
  features: [ubuntu, apache, placeholder]
- chain:
  - {rule: ShowcaseServer}
  - {name: ubuntu, tag: latest}
  features: [showcase, ubuntu]
- chain:
  - {rule: Postgres}
  - {name: ubuntu, tag: latest}
  features: [postgres, ubuntu, placeholder]
